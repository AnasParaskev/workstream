
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/workstream_db
    username: workstream
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
